// Code generated by generate-terraform-provider. DO NOT EDIT.

package provider

import (
	"errors"
	"fmt"
	"net"
	"net/http"
	"net/netip"
	"net/url"
	"strconv"
	"strings"
	"time"

	"golang.org/x/net/idna"
)

func validateAPIBaseURL(value string) (*url.URL, error) {
	if value == "" {
		return nil, errors.New("base_url must not be empty.")
	}

	endpoint, err := url.Parse(value)
	if err != nil || !endpoint.IsAbs() || endpoint.Hostname() == "" || endpoint.Opaque != "" || !validAPIEndpointAuthority(endpoint) {
		return nil, errors.New("base_url must be an absolute HTTP or HTTPS URL.")
	}
	if endpoint.User != nil {
		return nil, errors.New("base_url must not contain embedded credentials.")
	}
	if endpoint.Port() != "" {
		port, err := strconv.Atoi(endpoint.Port())
		if err != nil || port < 1 || port > 65535 {
			return nil, errors.New("base_url port must be between 1 and 65535.")
		}
	}

	switch endpoint.Scheme {
	case "https":
		return endpoint, nil
	case "http":
		host := endpoint.Hostname()
		if strings.EqualFold(host, "localhost") {
			return endpoint, nil
		}
		if address := net.ParseIP(host); address != nil && address.IsLoopback() {
			return endpoint, nil
		}
		return nil, errors.New("base_url must use HTTPS unless its host is localhost or a loopback IP address.")
	default:
		return nil, errors.New("base_url must use the HTTP or HTTPS scheme.")
	}
}

func validAPIEndpointAuthority(endpoint *url.URL) bool {
	hostname := endpoint.Hostname()
	if port := endpoint.Port(); port != "" {
		return strings.EqualFold(endpoint.Host, net.JoinHostPort(hostname, port))
	}
	if strings.Contains(hostname, ":") {
		return strings.EqualFold(endpoint.Host, "["+hostname+"]")
	}
	return strings.EqualFold(endpoint.Host, hostname)
}

func effectiveAPIOriginPort(endpoint *url.URL) int {
	if endpoint.Port() != "" {
		port, _ := strconv.Atoi(endpoint.Port())
		return port
	}
	if strings.EqualFold(endpoint.Scheme, "https") {
		return 443
	}
	return 80
}

func canonicalAPIOriginHost(endpoint *url.URL) (string, error) {
	if address, err := netip.ParseAddr(endpoint.Hostname()); err == nil {
		return address.String(), nil
	}
	return idna.Lookup.ToASCII(endpoint.Hostname())
}

func sameAPIOrigin(requestURL, endpoint *url.URL) bool {
	requestHost, err := canonicalAPIOriginHost(requestURL)
	if err != nil {
		return false
	}
	endpointHost, err := canonicalAPIOriginHost(endpoint)
	if err != nil {
		return false
	}
	return strings.EqualFold(requestURL.Scheme, endpoint.Scheme) &&
		requestHost == endpointHost &&
		effectiveAPIOriginPort(requestURL) == effectiveAPIOriginPort(endpoint)
}

func newCredentialAudienceHTTPClient(endpoint *url.URL) *http.Client {
	transport := http.DefaultTransport
	if defaultTransport, ok := transport.(*http.Transport); ok {
		clonedTransport := defaultTransport.Clone()
		clonedTransport.ResponseHeaderTimeout = 10 * time.Minute
		transport = clonedTransport
	}

	return &http.Client{
		Transport: transport,
		CheckRedirect: func(request *http.Request, previous []*http.Request) error {
			if !sameAPIOrigin(request.URL, endpoint) {
				return errors.New("redirect crosses the configured OpenAI API origin")
			}
			if len(previous) >= 10 {
				return fmt.Errorf("stopped after %d redirects", len(previous))
			}
			return nil
		},
	}
}
