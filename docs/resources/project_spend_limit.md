---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "openai_project_spend_limit Resource - openai"
subcategory: ""
description: |-
  Manage a project hard spend limit.
---

# openai_project_spend_limit (Resource)

Manage a project hard spend limit.

## Example Usage

```terraform
resource "openai_project_spend_limit" "example" {
  project_id       = "proj_123"
  threshold_amount = 20
  currency         = "USD"
  interval         = "month"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `currency` (String) currency
- `interval` (String) interval
- `project_id` (String) project id
- `threshold_amount` (Number) threshold amount

### Read-Only

- `enforcement_status` (String) enforcement status
- `object` (String) object

## Import

Import is supported using the following syntax:

In Terraform v1.5.0 and later, the [`import` block](https://developer.hashicorp.com/terraform/language/import) can be used with the `id` attribute, for example:

```terraform
import {
  to = openai_project_spend_limit.example
  id = "proj_123"
}
```
